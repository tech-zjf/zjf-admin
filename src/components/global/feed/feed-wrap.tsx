export interface FeedWrapProps {
    item: any,
}

const FeedWrap: React.FC<FeedWrapProps> = (props) => {
    const { item } = props

    return (
        <div>
            list
        </div>
    )
}
export default FeedWrap